import 'package:flutter/material.dart';

/// ======================================================================
/// PALETTE DE COULEURS DE LA BOURSE AGRICOLE NUMÉRIQUE (BAN)
/// ----------------------------------------------------------------------
/// Toutes les couleurs de l'application sont regroupées ici.
/// Cela évite de répéter les codes hexadécimaux partout.
/// ======================================================================

// Couleur principale (menus, boutons principaux...)
const Color couleurPrincipale = Color(0xFF334C50);

// Couleur secondaire (liens, boutons secondaires...)
const Color couleurSecondaire = Color(0xFF5654B8);

// Couleur utilisée pour les alertes ou erreurs
const Color couleurAlerte = Color(0xFFE9168B);

// Couleur utilisée pour les messages de succès
const Color couleurSucces = Color(0xFF1D7C59);

// Couleur de fond principale de l'application
const Color couleurFond = Color(0xFFFEFAE1);

// Couleur de fond des cartes et sections
const Color couleurFondSecondaire = Color(0xFFF2F3F5);